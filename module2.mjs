const myName = 'Rachana'
const myAge = 22
export {myAge}
export default myName

// As myName is default export, i can import it with any name not that i need to use myName only, but if i export myAge i can ex@category:debuggerst with named export because, there can only be one default export  in a file